package banghak.data.platform.hyperion.repository.entity

enum class ResultType {
    /** Data extraction — produces result.xlsx. */
    EXTRACT,

    /** Data visualization — produces visualization.html (d3.js). */
    VISUALIZE
}

